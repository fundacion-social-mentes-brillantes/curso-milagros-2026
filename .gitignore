# dependencias
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# producción
/build

# entorno (NUNCA subir claves)
.env
.env.local
.env*.local
serviceAccount.json
*serviceAccount*.json
firebase-admin-key*.json

# logs / sistema
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.pem

# vercel
.vercel

# typescript
*.tsbuildinfo

# videos (NUNCA subir los videos al repo: van en Google Drive/YouTube)
*.mp4
*.mov
*.webm
/Videos*/

# audio generado por TTS (pesa mucho; se aloja aparte, no en el repo)
/audio-generado/
tts-key.txt
gemini-key.txt
gemini-keys.txt

.vercel
.env*

# artefactos de despliegue a Azure
/despertador.zip
/funcion-avisos/node_modules
/api/node_modules

# Audios narrados: viven en Azure Blob, no en el repositorio.
# Son ~5 MB cada uno; las 365 lecciones serían más de un giga en git, y git
# guarda cada versión para siempre. Los .txt del guion sí se quedan: pesan nada
# y dejan constancia de QUÉ se narró exactamente.
audio-nuevo/*.mp3
audio-viejo-blog/
