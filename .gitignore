# dependencias
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# producción
/build

# entorno (NUNCA subir claves)
.env
.env.local
.env*.local
serviceAccount.json
*serviceAccount*.json
firebase-admin-key*.json

# logs / sistema
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.pem

# vercel
.vercel

# typescript
*.tsbuildinfo

# videos (NUNCA subir los videos al repo: van en Google Drive/YouTube)
*.mp4
*.mov
*.webm
/Videos*/

# audio generado por TTS (pesa mucho; se aloja aparte, no en el repo)
/audio-generado/
tts-key.txt
gemini-key.txt

.vercel
